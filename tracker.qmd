---
title: ""
format: 
    html:
        self-contained: true
editor: visual

execute:
  echo: false
  
font-size: 14px
---

### Progress Tracker

------------------------------------------------------------------------

```{r}
library(r2d3)
```

```{r}
dates <- seq(as.Date("2023-06-28"), as.Date("2023-08-15"), by="days")
```

```{r}
#| label: word-count-data
words <- c(975)
wordcount <- data.frame(day=dates, words=c(words, rep(0, length(dates)-length(words))))

ojs_define(wordcountdata = wordcount)
```

```{r}
#| label: pomodoro-data
sessions <- c(12)
pomodoro <- data.frame(day=dates, sessions=c(sessions, rep(-3, length(dates)-length(sessions))))

ojs_define(pomodorodata = pomodoro)
```

::: {layout-ncol="2"}
```{ojs}
wordcountdataParsed = transpose(wordcountdata)

Plot.plot({
  caption: "Number of words written per day 📝",
  x: {
    label: null
  },
  y: {
    grid: true,
    label: "Word Count",
    domain: [0,1000]
  },
  marks: [
    Plot.ruleY([0]),
    Plot.rectY(wordcountdataParsed,
    {x: (d) => new Date(d.day), 
    interval: d3.utcDay, 
    y: "words"})
    ]
})

```

```{ojs}
//| fontsize: 15

pomodorodataParsed = transpose(pomodorodata)

Plot.plot({
  caption: "Number of pomodoro sessions per day 🍅",
  x: {
    label: null
    },
  y: {
    label: "Sessions",
    domain: [0,14]
  },
  marks: [
    Plot.ruleY([0]),
    Plot.dot(pomodorodataParsed,
    {x: (d) => new Date(d.day), 
    interval: d3.utcDay, 
    y: "sessions",
    r: 6,
    fill: true})
    ]
})
```
:::

[800 words = 1 Conrad](https://www.theguardian.com/books/2016/jun/18/my-writing-day-will-self)
